{
  "name": "@develapp/opa-editor-backend",
  "version": "0.1.0",
  "description": "Backstage backend plugin — OPA Rego editor validation & distribution",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build",
    "test": "jest"
  },
  "dependencies": {
    "@develapp/opa-domain-contract": "^0.1.0"
  },
  "peerDependencies": {
    "@backstage/backend-plugin-api": "^1.0.0",
    "@backstage/plugin-permission-common": "^0.8.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "typescript": "^5.5.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["<rootDir>/src/__tests__/**/*.test.ts"]
  }
}
